# Mission drafts

Each mission is one directory under `Missions/`.

    Missions/<slug>/Statements.lean     every theorem item, each := by sorry
    Missions/<slug>/Definitions.lean    the definition item, if the mission needs one
    Missions/<slug>/proposal.json       name, type, fields, visibility, item_order
    Missions/<slug>/items/<name>.json   title, natural language statement, source, tags
    Missions/<slug>/milestones.json     item, title, description

The `item_order` field and `milestones.json` hold names rather than ids, because a server item id exists only after `p2m item-add` has run. A definition item is a whole module rather than a single declaration: `p2m item-add --kind definition` uploads the whole `Definitions.lean`, and `definition_name` names the module the platform serves back.

## Building

A mission slug is hyphenated, so the module component needs French quotes:

    lake build 'Missions.«shao-three-units».Statements'

`Missions` is a Lake library and is not in `defaultTargets`, so a bare `lake build` stays small. The gate policy gives this tree `statements` mode: a draft item must end in `:= by sorry`, and `statements` still refuses a hand-written `axiom`, a `native_decide`, and a compile error.

## The driver

`./upload.py <slug> <step>` runs the upload sequence, resolving the proposal id from the ledger.

    ./upload.py <slug> sweep      the duplicate sweep the gate demands
    ./upload.py <slug> dry        every item, exits nonzero if any refuses
    ./upload.py <slug> propose    create the draft
    ./upload.py <slug> items      add every item, in item_order
    ./upload.py <slug> finish     read-backs, then milestones
    ./upload.py <slug> order      set item_order and the goal

Run `dry` and see exit 0 before `propose`, every time. `submit`, `explain`, and `field-new` still require a human at a terminal, because each writes to something a later delete cannot undo.

## Status

All four went up as complete drafts and have moved out of `Draft`. The figures below were read on 2026-09-23; re-read them with `p2m proposal <id>`.

| Mission | Items | Platform status | Proved | Open | Proposal id |
|---|---|---|---|---|---|
| `shao-three-units` | 10 theorems | Reviewed | 10 | 0 | `dc6c62ca-47f1-47e0-ad81-63ff92dc655f` |
| `z2n-five-eighths` | 1 definition, 10 theorems | Reviewed | 9 | 1 | `4c610437-1abb-40bf-9e2c-e0427f7b17a1` |
| `sidon-sqrt-n` | 17 theorems | Reviewed | 17 | 0 | `75753f60-2f7d-4a4a-b4e8-ea67c9106112` |
| `konyagin-unit-vectors` | 2 theorems | Changes requested | 0 | 2 | `591a6c72-bbae-444d-8ddb-e4b16a94e039` |

`konyagin-unit-vectors` came back from review with one requested change: the lower-bound item said "for arbitrarily large `n`", where Alon's Theorem 3.1 gives the bound for every `n`. The item now quantifies every `n`, as the moderator's suggested statement does. A confirmed item is a reference the platform will not patch, so on 2026-09-23 the old item was removed and the fixed one added in its place, with its read-back and milestone. Re-submit for review is a web action and the last step.

Other contributors proved every item of [`shao-three-units`](https://prove2.me/missions/Shao's_three_units_theorem%3A_density_5%2F8_forces_a_three-fold_additive_basis) and [`sidon-sqrt-n`](https://prove2.me/missions/The_Komlos-Sulyok-Szemeredi_bound%3A_every_finite_set_of_reals_has_a_Sidon_subset_of_size_c_sqrt_n) on the platform, and every accepted proof came from someone else: our ledger lists zero submissions to either mission. That is the platform's report, never a verdict here. The finished proofs in `shao-three-units/Development/` still audit clean locally, and no item now needs them.

`z2n-five-eighths/Research/` holds the SAT reproduction of `f(7) = 80`, its three referee reports, and the instrument that produced it. The UNSAT half is still uncertified, and the mission rests on neither half.

`prose.md` in each directory is generated by `extract-prose.py` so that `llm_tics.py --repo` can lint prose inside json. Regenerate after editing any json, and run `./extract-prose.py --check` to catch drift.
